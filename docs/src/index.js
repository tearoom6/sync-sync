import React from 'react'
import ReactDOM from 'react-dom'
import Layout from './Layout'

import css from './sync-sync'
import css_docs from './sync-sync-docs'

ReactDOM.render(
  <Layout />,
  document.getElementById('root')
)
